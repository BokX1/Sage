name: Release Supply Chain

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read
  id-token: write
  attestations: write

jobs:
  bundle-and-attest:
    uses: ./.github/workflows/_release-supply-chain-job.yml
